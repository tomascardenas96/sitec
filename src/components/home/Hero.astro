---
import hero from "../../assets/images/f5d41eb7-25c9-48a8-8e4f-95947947fa13.png";
const phone = "5492281378525";
const text =
  "Buen día, quisiera solicitar un presupuesto para la instalación de paneles eléctricos en mi empresa. Muchas gracias.";
const encoded = encodeURIComponent(text);
---

<section
  class="h-96 lg:h-[19rem] relative flex items-center justify-center bg-cover bg-center flex-col gap-8 lg:gap-5"
  style={`background-image: url('${hero.src}');`}
>
  <div class="absolute inset-0 bg-black/50"></div>

  <!-- Mobile hero title -->
  <div class="p-6 border-2 border-white z-10 rounded-[.2rem] lg:hidden">
    <h1
      class="relative text-white text-[1.7rem] tracking-[0.02em] font-[400] z-10 flex text-center font-oswald max-w-[20ch]"
    >
      SOLUCIONES ELECTRICAS E INDUSTRIALES
    </h1>
  </div>

  <!-- Desktop hero title -->
  <div class="z-10 rounded-[.2rem] hidden pt-8 lg:block">
    <h1
      class="w-[35ch] relative text-white text-[1.8rem] font-[300] z-10 flex text-center font-oswald leading-9 tracking-[.04em]"
    >
      Soluciones eléctricas integrales y montajes industriales
    </h1>
  </div>

  <button
    class="bg-btn relative cursor-pointer z-10 text-sm lg:text-xs flex h-12 w-52 lg:h-10 lg:w-44 items-center gap-2 justify-center rounded-md shadow-xl text-white font-oswald font-medium lg:font-normal"
  >
    <a
      href={`https://wa.me/${phone}?text=${encoded}`}
      target="blank"
      class="h-full w-full text-white flex items-center justify-center"
      >SOLICITAR PRESUPUESTO</a
    >
  </button>
</section>

<style>
  .bg-btn {
    background: linear-gradient(to right, #e48322, #a76420);
  }
</style>
