---
import texture from "../../assets/images/textura.png";
import SectionTitle from "../common/Section-title.astro";
import Like from "../../assets/icons/like.svg";
import { testimonials } from "../../data/testimonials";
import Testimonial from "./Testimonial.card.astro";
---

<section
  class="mt-2 py-10 px-5 bg-cover pb-20 xl:pb-24 md:flex md:flex-col md:items-center"
  style={`background-image: url('${texture.src}')`}
>
  <SectionTitle
    label="Testimonios de Clientes"
    subtitle="Experiencia, innovación y resultados comprobados en cada uno de nuestros trabajos"
    color="white"
    hasIcon={true}
  >
    <Like slot="icon" />
  </SectionTitle>

  <div
    class="grid md:flex gap-6 md:gap-8 lg:gap-6 xl:gap-8 mt-8 md:mt-10 md:max-w-[42.5rem] lg:max-w-[50rem] xl:max-w-[60rem] lg:w-[50rem] xl:w-[60rem] md:overflow-auto lg:overflow-visible lg:grid lg:grid-cols-3"
  >
    {
      testimonials.map((testimonial) => (
        <Testimonial
          image={testimonial.image}
          name={testimonial.name}
          body={testimonial.body}
        />
      ))
    }
  </div>
</section>
